version: '3.8'

services:
  # Redis for message queue, state management, and caching
  redis:
    image: redis:7-alpine
    container_name: linkedin-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - linkedin-network

  # Main LinkedIn automation application
  linkedin-app:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-automation
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
      - LINKEDIN_HEADLESS=true
      - LINKEDIN_BROWSER_TYPE=playwright
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - linkedin_sessions:/app/sessions
    ports:
      - "8080:8080"  # Web dashboard (if enabled)
    networks:
      - linkedin-network
    healthcheck:
      test: ["CMD", "python", "-c", "import asyncio; import sys; sys.path.insert(0, 'src'); from src.infrastructure.health_check import health_check; asyncio.run(health_check())"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Account Manager Agent (dedicated instance)
  account-manager:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-account-manager
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
      - LINKEDIN_HEADLESS=true
      - LINKEDIN_BROWSER_TYPE=playwright
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - account_sessions:/app/sessions
    command: ["--agents", "account_manager"]
    networks:
      - linkedin-network

  # Content Analysis Agent (dedicated instance)
  content-analyzer:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-content-analyzer
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["--agents", "content_analysis"]
    networks:
      - linkedin-network

  # Interaction Agent (dedicated instance)
  interaction-agent:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-interaction-agent
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      account-manager:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
      - LINKEDIN_HEADLESS=true
      - LINKEDIN_BROWSER_TYPE=playwright
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - interaction_sessions:/app/sessions
    command: ["--agents", "interaction"]
    networks:
      - linkedin-network

  # Conversation Agent (dedicated instance)
  conversation-agent:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-conversation-agent
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      content-analyzer:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["--agents", "conversation"]
    networks:
      - linkedin-network

  # Safety Agent (dedicated instance)
  safety-agent:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-safety-agent
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["--agents", "safety"]
    networks:
      - linkedin-network

  # Scheduler -- what makes the programme autonomous.
  #
  # This service used to run `main.py --agents scheduler`, which booted
  # SchedulerAgent: a SkeletonAgent whose _start_agent_tasks returns []. It came
  # up healthy and did nothing, which is worse than not existing -- anyone
  # reading this file concluded scheduling was handled.
  #
  # It now runs the real scheduler (src/scheduler). Off unless SCHEDULER_ENABLED
  # is set, and it exits non-zero with an explanation rather than idling, so a
  # misconfiguration is a visibly failing container instead of a quiet one.
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-scheduler
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      # The app container runs scripts/migrate.py before serving, so waiting on
      # it keeps the scheduler from querying a schema that does not exist yet.
      # Not fatal if it did -- the loop survives a failed sweep and retries --
      # but a clean first tick beats a confusing one in the logs.
      linkedin-app:
        condition: service_started
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=${DATABASE_URL:-}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - LOG_LEVEL=INFO
      # Deliberately off by default: this drives real activity on real accounts.
      - SCHEDULER_ENABLED=${SCHEDULER_ENABLED:-false}
      - SCHEDULER_DRY_RUN=${SCHEDULER_DRY_RUN:-false}
      - SCHEDULER_INTERVAL_SECONDS=${SCHEDULER_INTERVAL_SECONDS:-300}
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["python", "-m", "src.scheduler"]
    networks:
      - linkedin-network

  # Analytics Agent (dedicated instance)
  analytics-agent:
    build: 
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: linkedin-analytics
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - LOG_LEVEL=INFO
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["--agents", "analytics"]
    networks:
      - linkedin-network

  # Monitoring with Prometheus (optional)
  prometheus:
    image: prom/prometheus:latest
    container_name: linkedin-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    networks:
      - linkedin-network
    profiles:
      - monitoring

  # Grafana for visualization (optional)
  grafana:
    image: grafana/grafana:latest
    container_name: linkedin-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    depends_on:
      - prometheus
    networks:
      - linkedin-network
    profiles:
      - monitoring

volumes:
  redis_data:
  linkedin_sessions:
  account_sessions:
  interaction_sessions:
  prometheus_data:
  grafana_data:

networks:
  linkedin-network:
    driver: bridge
    name: linkedin-automation-network